package main

import (
	"fmt"
)

func main() {
	name := "booboo"
	surname := "licious"
	age := 28
	// message := name + surname + " " + string(age)
	fmt.Println(name+surname, age, "years of the lucosete")
}
